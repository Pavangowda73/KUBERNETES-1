apiVersion: apps/v1
kind: Deployment
metadata:
  name: pay-deployment
  labels:
    app: pay-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pay-deploy
  template:
    metadata:
      labels:
        app: pay-deploy
    spec:
      containers:
      - name: pay-deploy
        image: pavan73384/ecommerce:payment
        ports:
        - containerPort: 4000
---

apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  selector:
    app: pay-deploy
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
