apiVersion: v1
kind: Service
metadata:
  name: supermarket-service
spec:
  selector:
    app: frontend-supermarket
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
