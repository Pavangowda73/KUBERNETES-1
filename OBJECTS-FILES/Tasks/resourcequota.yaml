apiVersion: v1
kind: ResourceQuota
metadata:
  name: techmart-prod-quota
  namespace: techmart-prod
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "4"
    limits.memory: 8Gi
