apiVersion: v1
kind: ResourceQuota
metadata:
  name: techmart-quota
  namespace: techmart-dev

spec:
  hard:
    requests.cpu: "4"
    limits.cpu: "4"

    requests.memory: 8Gi
    limits.memory: 8Gi
